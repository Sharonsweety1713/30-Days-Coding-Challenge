class Solution {
    public boolean isPalindrome(String s) {
         
        String os = s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
        String rs = new StringBuilder(os).reverse().toString();

        return os.equals(rs);
    }
}
